import datetime
from django.db import models
from django.contrib.auth.models import User

#
#from django.utils import timezone
#

# Create your models here.
# class Affirmations(models.Model):
    #affirmationText = models.Charfield(max_length=100)
    #categoryID = models.IntegerField()
    #categoryName = models.Charfield(max_length=30)
    #categoryName = models.ForeignKey()
    #createdDate = models.DateField(default=tdatetime.datetime.now)
    #slug = models.SlugField(default="")

    #useful if wanting to print objects in the shell
    #def _str_(self):

    #class Meta:
    #Ascending by date published
    #ordering = ['createdDate']
